# Minimal image for the tremulator network lab: shaping (iproute2/tc),
# measurement (iperf3, ping) and capture (tcpdump).
FROM alpine:3.20
RUN apk add --no-cache iproute2 iproute2-tc iperf3 iputils tcpdump bash coreutils
CMD ["sleep", "infinity"]
