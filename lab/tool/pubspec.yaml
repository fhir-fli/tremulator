name: tremulator_lab
description: Test instruments for tremulator's Gate 1 (network profiles, adversary harness). Not shipped.
publish_to: none
environment:
  sdk: ^3.5.0
dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.0.0
